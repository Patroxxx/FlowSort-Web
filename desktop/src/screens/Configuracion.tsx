import{useState,type FormEvent}from"react";import type{ApiClient,Sesion}from"@flowsort/shared";
export function Configuracion({api,sesion,setSesion}:{api:ApiClient;sesion:Sesion;setSesion:(s:Sesion)=>void}){const[modo,setModo]=useState<"GOOGLE_CLOUD"|"AUTOALOJADO">("GOOGLE_CLOUD"),[mensaje,setMensaje]=useState("");async function marca(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));const empresa=await api.request<Sesion["empresa"]>("/empresa",{method:"PUT",body:JSON.stringify({...d,logoUrl:d.logoUrl||null})});setSesion({...sesion,empresa});setMensaje("Marca actualizada")};async function hosting(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));await api.request("/empresa/hosting-db",{method:"PUT",body:JSON.stringify(d)});setMensaje("Hosting configurado")};return <><header><div><h1>Configuración</h1><span>Marca y almacenamiento</span></div></header>{mensaje&&<div className="notice">{mensaje}</div>}<div className="settings"><form className="panel form" onSubmit={marca}><h2>Empresa</h2><label>Nombre<input name="nombre" defaultValue={sesion.empresa.nombre} required/></label><label>URL del logo<input name="logoUrl" type="url" defaultValue={sesion.empresa.logoUrl??""}/></label><button>Guardar marca</button></form><form className="panel form" onSubmit={hosting}><h2>Base operativa</h2><label>Modo<select name="modoHostingDB" value={modo} onChange={e=>setModo(e.target.value as typeof modo)}><option value="GOOGLE_CLOUD">Google Cloud gestionado</option><option value="AUTOALOJADO">PostgreSQL autoalojado</option></select></label>{modo==="AUTOALOJADO"&&<label>Cadena PostgreSQL<input name="conexion" type="password" placeholder="postgresql://…" required/></label>}<p className="muted">Credenciales autoalojadas cifradas antes de persistir.</p><button>Aplicar hosting</button></form></div></>}
