import{useEffect,useMemo,useState}from"react";import{ApiClient,type Sesion}from"@flowsort/shared";import{Auth}from"./screens/Auth";import{Inicio}from"./screens/Inicio";import{Registros}from"./screens/Registros";import{Empleados}from"./screens/Empleados";import{Configuracion}from"./screens/Configuracion";
const API=import.meta.env.VITE_API_URL??"http://localhost:4000/api";type Vista="inicio"|"registros"|"empleados"|"monitoreo"|"configuracion";
export default function App(){const[sesion,setSesion]=useState<Sesion|null>(()=>JSON.parse(localStorage.getItem("flowsort.session")??"null"));const[vista,setVista]=useState<Vista>("inicio");const api=useMemo(()=>new ApiClient(API,()=>setSesion(null)),[]);useEffect(()=>{api.setSession(sesion);if(sesion)localStorage.setItem("flowsort.session",JSON.stringify(sesion));else localStorage.removeItem("flowsort.session")},[sesion]);useEffect(()=>{const saved=JSON.parse(localStorage.getItem("flowsort.session")??"null") as Sesion|null;if(!saved)return;api.setSession(saved);void api.request("/plantillas").catch(()=>setSesion(null))},[]);if(!sesion)return <Auth api={api} onAuth={setSesion}/>;const admin=sesion.usuario.rol==="ADMIN_EMPRESA";return <div className="shell"><aside><div className="brand">{sesion.empresa.logoUrl?<img src={sesion.empresa.logoUrl}/>:<img className="app-logo" src="/flowsort-logo.png"/>}<span>{sesion.empresa.nombre}</span></div><nav><button className={vista==="inicio"?"active":""} onClick={()=>setVista("inicio")}>Inicio</button><button className={vista==="registros"?"active":""} onClick={()=>setVista("registros")}>Registros</button>{admin&&<><button className={vista==="empleados"?"active":""} onClick={()=>setVista("empleados")}>Empleados</button><button className={vista==="monitoreo"?"active":""} onClick={()=>setVista("monitoreo")}>Monitoreo / Exportar</button><button className={vista==="configuracion"?"active":""} onClick={()=>setVista("configuracion")}>Configuración</button></>}</nav><button className="logout" onClick={()=>setSesion(null)}>Cerrar sesión</button></aside><main>{vista==="inicio"&&<Inicio api={api} sesion={sesion} onNavigate={setVista}/>} {vista==="registros"&&<Registros api={api}/>} {vista==="monitoreo"&&<Registros api={api} monitoreo/>}{vista==="empleados"&&<Empleados api={api}/>} {vista==="configuracion"&&<Configuracion api={api} sesion={sesion} setSesion={setSesion}/>}</main></div>}
