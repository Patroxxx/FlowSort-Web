import type{CampoPlantilla}from"@flowsort/shared";export function DynamicFields({campos,valores,onChange}:{campos:CampoPlantilla[];valores:Record<string,unknown>;onChange:(v:Record<string,unknown>)=>void}){return <div className="dynamic">{campos.map(c=><label key={c.id}>{c.etiqueta}{c.requerido&&" *"}{c.tipo==="textarea"?<textarea required={c.requerido} value={String(valores[c.id]??"")} onChange={e=>onChange({...valores,[c.id]:e.target.value})}/>:c.tipo==="select"?<select required={c.requerido} value={String(valores[c.id]??"")} onChange={e=>onChange({...valores,[c.id]:e.target.value})}><option value="">Seleccionar</option>{c.opciones?.map(o=><option key={o}>{o}</option>)}</select>:c.tipo==="checkbox"?<input type="checkbox" checked={Boolean(valores[c.id])} onChange={e=>onChange({...valores,[c.id]:e.target.checked})}/>:<input required={c.requerido} type={c.tipo==="numero"?"number":c.tipo==="fecha"?"date":c.tipo==="fecha_hora"?"datetime-local":c.tipo==="email"?"email":c.tipo==="telefono"?"tel":"text"} value={String(valores[c.id]??"")} onChange={e=>onChange({...valores,[c.id]:c.tipo==="numero"&&e.target.value!==""?Number(e.target.value):c.tipo==="fecha_hora"&&e.target.value?new Date(e.target.value).toISOString():e.target.value})}/>}</label>)}</div>}
