import { app,BrowserWindow,dialog,ipcMain } from "electron";import { join } from "node:path";import { writeFile } from "node:fs/promises";
function createWindow(){const win=new BrowserWindow({width:1280,height:820,minWidth:900,minHeight:620,backgroundColor:"#0B1020",title:"FlowSort",webPreferences:{preload:join(__dirname,"preload.js"),contextIsolation:true,nodeIntegration:false}});const dev=process.env.VITE_DEV_SERVER_URL;if(dev)void win.loadURL(dev);else void win.loadFile(join(__dirname,"../dist/index.html"));}
ipcMain.handle("save-file",async(_e,name:string,data:ArrayBuffer)=>{const r=await dialog.showSaveDialog({defaultPath:name,filters:[{name:"Excel",extensions:["xlsx"]}]});if(r.canceled||!r.filePath)return false;await writeFile(r.filePath,Buffer.from(data));return true;});app.whenReady().then(createWindow);app.on("window-all-closed",()=>{if(process.platform!=="darwin")app.quit()});app.on("activate",()=>{if(BrowserWindow.getAllWindows().length===0)createWindow()});
